require("bootstrap");
require("@fortawesome/fontawesome-free");
const AOS = require("aos");
// Starting Animate On Scroll (AOS)
AOS.init({
  delay: 200,
  duration: 1000,
  once: true,
  mirror: false,
});
